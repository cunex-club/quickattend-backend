name: Build and Test

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches-ignore: [main]
  workflow_dispatch:
  workflow_call:

jobs:
  build-test:
    concurrency:
      group: build-test-${{ github.action_ref }}
      cancel-in-progress: true
    name: Build and Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.28"

      - name: Install dependencies
        run: go mod tidy

      - name: Run tests
        run: go test ./test/...

      - name: Build application
        run: go build -o app ./cmd/server
